{
  "type": "object",
  "additionalProperties": false,
  "required": ["verdict", "score", "feedback", "understoodConcepts", "missingConcepts", "followUpQuestion"],
  "properties": {
    "verdict": {"type": "string", "enum": ["CORRECT", "PARTIAL", "INCORRECT"]},
    "score": {"type": "integer", "minimum": 0, "maximum": 100},
    "feedback": {"type": "string", "minLength": 10, "maxLength": 600},
    "understoodConcepts": {"type": "array", "minItems": 0, "maxItems": 6, "items": {"type": "string", "minLength": 2, "maxLength": 200}},
    "missingConcepts": {"type": "array", "minItems": 0, "maxItems": 6, "items": {"type": "string", "minLength": 2, "maxLength": 200}},
    "followUpQuestion": {"type": "string", "minLength": 0, "maxLength": 500}
  }
}
